spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

server:
  # IP real do cliente atrás do Nginx (limite de login e auditoria); validado na Fase 11.
  forward-headers-strategy: native
  servlet:
    session:
      cookie:
        secure: true
