app:
  # O job roda de verdade às 00:15 em APP_TIMEZONE; nos testes ele é chamado diretamente (D-091).
  jobs:
    enabled: false
  bootstrap-admin:
    email: admin@test.local
    password: test-admin-password
spring:
  jpa:
    properties:
      hibernate:
        # Só nos testes: prova que o dashboard não carrega entidades (D-098).
        generate_statistics: true
